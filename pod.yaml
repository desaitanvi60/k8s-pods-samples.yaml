apiVersion: v1
kind: Pod
metadata:
  name: redis-pod                 
spec:
  containers:                     
    - name: redis                  # you can use any name 
      image: redis                 # Use the official Redis image

